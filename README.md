# FullStack TODO — Task Manager

A full-stack task manager with JWT authentication, built with React (Vite) and Express + MongoDB.

## Features
- JWT signup/login (bcrypt password hashing)
- Protected routes (frontend + backend)
- Full CRUD for tasks
- Token auto-attached to API requests

## Tech Stack
React, Express.js, MongoDB, Mongoose, JWT, bcrypt
